# Custom Skills

Place repo-authored skills here.

Guidelines:

- keep one directory per skill
- include a `SKILL.md`
- add a matching entry in `codex/manifest.json`
- avoid reusing a vendored upstream skill name unless you intend to replace it
