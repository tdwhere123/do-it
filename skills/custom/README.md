# Custom Skills

Place repo-authored skills here when they are not part of the default do-it
install surface.

Guidelines:

- keep one directory per skill
- include a `SKILL.md`
- add a matching entry in `manifest.json` only when it should be installed
- avoid reusing an installed do-it skill name unless you intend to replace it
